[testServer]
BROKER_HOST = 192.168.191.18  
BROKER_PORT = 5672            
USER = ccag                   
PASSWORD = ccag              
VHOST = ccagHost              

; Database Server Configuration
DATABASE_EXCHANGE = databaseExchange
DATABASE_QUEUE = databaseQueue
DATABASE_ROUTING_KEY = user.*

; Client Configuration
CLIENT_EXCHANGE = clientExchange
CLIENT_QUEUE = clientQueue
CLIENT_ROUTING_KEY = response.*

; Exchange Type (topic for both exchanges)
EXCHANGE_TYPE = topic

; Auto-delete queues (set to false for durability)
AUTO_DELETE = false
